# 盘古舆情 — Python Engine dependencies
# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.0

# Web scraping — Scrapling (BSD-3-Clause)
# Install: pip install scrapling[fetchers] && scrapling install
scrapling[fetchers]

# Document generation
python-docx>=1.1.0

# Testing
pytest>=8.0
pytest-asyncio>=0.24.0