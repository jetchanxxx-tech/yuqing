# 行尾规范 —— 服务器部署直接跑 bash/systemd/Python，必须 LF。
# Windows git archive 在 core.autocrlf=true 下会把 LF 转成 CRLF，
# 导致服务器上 `set -euo pipefail` 解析失败（实测 deploy.sh 报
# "pipefail: invalid option name"）。显式 eol=lf 强制 archive/checkout 均为 LF。
*.sh text eol=lf
*.conf text eol=lf
*.service text eol=lf
*.py text eol=lf
*.go text eol=lf
*.ts text eol=lf
*.tsx text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.html text eol=lf
*.md text eol=lf
