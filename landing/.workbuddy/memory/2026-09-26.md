# 2026-09-26

## 盘古舆情落地页改版
- 基于 `landing/index.html` 重做单文件版本 → `landing/index-redesign.html`（内联 CSS + 原生 JS，无外部依赖）。
- 设计系统：主色 #2B5CE6→#6C4BE6 蓝紫渐变，辅助色 #22D3EE 青 / #F5B942 琥珀，中性 Ink 深色（#070B18 系）+ Slate 浅色；8px 间距基线，clamp() 字号刻度。
- 结构：导航 / Hero（深色+纯 CSS 产品界面模拟）/ 对比表 / 功能卡 / 优势（深色带）/ 定价 / 演示 / 技术架构 / FAQ 手风琴 / CTA / 页脚，明暗交替形成节奏。
- 交互：移动端汉堡菜单、FAQ 单开手风琴（max-height + aria-expanded）、IntersectionObserver 滚动入场、导航滚动态、prefers-reduced-motion 降级。
- 修复原页问题：FAQ 锚点缺 id（已补 `id="faq"`）、移动端导航脚本逻辑失效（改为 CSS + JS class 切换）、对比表移动端改为堆叠卡片（CSS ::before 补列标签）。
- 原文案与全部外链（GitHub / yuqing2 注册登录 / mailto / ../demo/index.html）保持未改动。

## 改版 v2：全站浅色统一（用户反馈"底色突然变白、风格不统一"）
- 原明暗交替（深 Hero→白→深优势带→白→深 CTA）交界硬切 → 改为全站浅色基调。
- 新底色三级：#FFFFFF / #F6F8FD / #EEF3FC；文字 #0D1526 / #5A6885；分隔线 #E3E9F5。
- 深色仅保留三处：产品截图窗口（置于白色卡框 .mock-frame 内）、CTA 收尾区、页脚（CTA 与页脚同色 #070B18 无缝）。
- 新增 .fade-to-dark 过渡桥（132px，浅灰→#070B18 渐变）消除浅→深硬切；移动端降至 96px。
- 辅助色在浅底上需用深青 #0E7490（--accent-700）做文字级，#22D3EE 仅用于装饰渐变。
- 用户偏好：视觉基调倾向全站浅色统一，不喜明暗反复切换。

## 改版 v3：套用 design/ 设计规范配色（小红书 RED）
- 权威来源：`D:\dev\舆情监测\design\design-spec.md` §3 色彩 Token + `shared.css` L1（两者一致，无冲突）；`index.html` 是设计系统文档中心（入口/主页面），11 个原型页取色频次验证主色。
- 提取色板：主色 #ff2442（hover #ff2e4d / active #e6203a）；语义 success #02b940（底 #eaf8ef）、warn #ff7d03（底 #fff2e6）、danger=#accent；画布 #f5f5f5、卡片 #ffffff、暖面 #fafafa；文字 rgba(0,0,0,.80/.62/.45/.27)；中性填充 rgba(48,48,52,.05/.10/.16/.20)；分隔 rgba(0,0,0,.08/.05)。
- 关键约束（规范 §2 禁忌）：品牌红不做渐变、不用紫/蓝渐变做主色、卡片不加左侧彩色竖条、近乎零阴影（hover 仅 0 4px 12px rgba(0,0,0,.08)）、每屏红 ≤2 处。
- 落地替换：删除全部蓝紫/青渐变；CTA 与页脚由深色改为 #fafafa / #fff（设计稿为全浅色系统，无深色块），因此移除 .fade-to-dark 过渡桥；Hero 内的产品截图 mock 由深色改为浅色工作台（中性灰柱 + 峰值红 + 情感三色环）。
- 对比度修正：rgba(0,0,0,.45) 实测仅 3.3:1，正文级说明（stat-label / price-period / 对比表列头与传统列 / tech p / demo p / .muted / 移动端列标签）统一提到 --fg-2（6.0:1）；--muted 仅保留于占位截图内部、页脚版权、✗ 图标。
- 沿用未改：间距、圆角、字号、字体栈、动效、组件结构、全部文案与外链。

## 回滚：v3 红色版被否决，回到 v2 蓝紫浅色版
- 用户反馈"很多表格被缩小得不能正常显示" → 放弃 v3，恢复 v2（蓝紫 #2B5CE6→#6C4BE6 + 青 #22D3EE，全站浅色 + 深色 CTA/页脚 + .fade-to-dark 过渡桥）。
- v3 已备份为 `index-redesign-v3-red.html`（如需对比或再次启用直接改名即可）。
- 重要教训：改版前先备份当前版本，或存为带版本后缀的文件；本项目 landing 页无 git 记录（index-redesign.html 未提交），覆盖后只能靠手工重建。
- 待确认："表格缩小"疑似预览面板宽度触发响应式断点（≤1080px 定价 4→2 列；≤768px 对比表堆叠为单列），与配色版本无关，v2/v3 表现一致。代码内无 zoom/scale 全局缩放。若确为拥挤，可选修复：定价卡改 2×2、容器放宽、对比表加 min-width + 横向滚动。

## 改版 v4：保留 v2 架构 + 换成设计稿红色（最终采用）
- 做法：token 名称体系沿用 v2（--brand-* / --bg* / --text-* / --ink-*），只替换取值；DOM、选择器、栅格、间距、圆角、动效一字未改。校验：9 个 section、21 个 article、.fade-to-dark 仍在，无蓝紫/青残留。
- 取值映射：brand-600=#ff2442、brand-500=#ff2e4d(hover)、brand-700=#e6203a(active/高对比红字)、brand-400=rgba(255,36,66,.35)；gold→warn #ff7d03/#fff2e6；success #02b940/#eaf8ef；bg=#fff(卡)、bg-soft=#f5f5f5(画布)、bg-tint=#fafafa(暖面)；text-900/800/700/500=rgba(0,0,0,.80/.62/.62/.62)，text-400=.45 仅用于 ✗ 图标；ink 系列改中性深墨 #0b0c0f/#141519/#1c1d22（去掉蓝调）；阴影压到 --sh-1 .04/.03、--sh-2 .07、--sh-3 .12。
- 规范禁忌落地：品牌红一律纯色（title-rule / 按钮 / badge / 竖条均去渐变）；深色区（截图窗口、CTA、页脚、过渡桥）按"架构不动"保留，光晕由紫+青换成红色双强度，底色转中性深墨。
- 截图窗口内：柱子改中性白阶、峰值第 6 根用红、侧栏激活项红、平台 chip 中性 + 热点 chip 红；情感环形用设计稿三色（正 #02b940 / 中 rgba(48,48,52,.18) / 负 #ff2442）。
- 对比度：正文 .62 在三种浅底上 6.0–6.2:1 达标；12px 徽标红字 3.76:1、success/warn 徽标 2.3–2.6:1 均沿用设计稿既定组合（规范 §6.5 状态徽标表），未擅改色值。

## 改版 v6：7 项内容修订（v4 之上，架构与配色均不动）
1. 演示入口链接统一到 https://yuqing2.pangu-cloud.com/demo.html：Hero「观看演示」+ #demo 区块「查看完整演示 →」（原相对路径 ../demo/index.html 已废弃，单文件部署会 404）。
2. 核心功能 6 卡删除时间/状态标签（即将上线/已上线/开发中）。注意：定价 Pro 卡的「多 Agent 辩论（即将上线）」与 FAQ 第 3 项的「开发中」属套餐与问答正文，按要求保留。
3. 优势卡删除 01–05 序号水印（.adv-no CSS 规则一并清理）。
4. 定价 4 卡外框等高：.grid-price 改 align-items:stretch、.price--hot 去掉 translateY(-10px) 常驻上浮、hover 由 -16px 统一为 -6px；按钮 margin-top:auto 保证底部对齐。
5. 整站删除「技术架构」章节（HTML + 对应 CSS 规则 .grid-tech/.tech*，共 19 行一并清理）。导航与页脚本无该章节入口。
6. FAQ 第 2 项只留「多 Agent 辩论是什么？」，删除「什么时候上线」及「预计 v0.2.0 版本上线（1-2 个月内）」。
7. 案例区（用户确认方案 A）：#cases 对比表内容不动，导航「案例」改「优势」、锚点 id 改 #compare；#demo 副标题由「真实案例："雅阁后排"舆情事件全流程分析」改为「预约演示，或注册后亲自体验一次完整分析流程」。
- 连带修复：删除技术架构后 demo(#fafafa) 与 faq(#fafafa) 相邻同色，把 FAQ 改为 section--light(#f5f5f5) 恢复交替。
- 校验：结构 0 错误、失效锚点 0、section 8 个、外链 16 条。经验：删除中间章节后必须检查相邻区块底色交替是否被打破。
